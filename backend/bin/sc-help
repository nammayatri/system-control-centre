#!/usr/bin/env bash
echo ""
echo "  System Control Centre"
echo "  ─────────────────────"
echo "  sc-dev         Start everything (Postgres + DB init + backend ghcid + frontend)"
echo "  sc-build       Compile the backend (cabal build)"
echo "  sc-test        Run test suite (cabal test)"
echo "  sc-test-api    Run API integration tests (server must be running)"
echo "  sc-format      Format all Haskell files via treefmt (fourmolu)"
echo "  sc-help        Show this help"
echo ""
echo "  Postgres data: ./.local/data/pg  (delete to reset DB)"
echo "  Backend port:  ${PORT:-8012}"
echo "  Frontend port: 5173"
echo "  DB URL:        ${SC_DATABASE_URL:-postgres://\$(whoami)@127.0.0.1:5434/system_control}"
echo ""
